using EmuLib.Utils.Reflection;

namespace EmuLib.Utils.HTTP
{
    internal static class LoginUtils
    {
        public static void SendLoginToken()
        {
            /*var token = new {token = MainAppUtils.GetTokenLine()}.ToJson();
            new HttpUtils.Create().Post("/launcher/game/login", token, true, response =>
            {
                
            });*/
        }
    }
}